#!/bin/sh

######################################################################
pkgbase=libxml2
vers=2.10.3
url="https://gitlab.gnome.org/GNOME/libxml2/-/archive/v${vers}/libxml2-v${vers}.tar.gz"
apply_arch="x86_64 i686 armv7l aarch64"
arch=`uname -m`
build=3
src=$pkgbase-v$vers
OPT_CONFIG="--with-history --disable-static PYTHON=/usr/bin/python3 --with-icu"
DOCS="AUTHORS COPYING ChangeLog Copyright INSTALL NEWS README README.tests TODO TODO_SCHEMAS"
patchfiles=""
compress=txz
SRC_URL=${SRC_URL:-"https://qbilinux.org/pub/source/"}
SRC_DIR=${SRC_DIR:-"/home/archives/source/"}
######################################################################

source /usr/src/qbilinux/PackageBuild.def

do_prepare() {
    cd ${S[$1]}
    for patch in $patchfiles ; do
	patch -p1 < $W/$patch
    done
    sed -i '/_PyVerify_fd/,+1d' python/types.c
}

do_config() {
    if [ -d ${B[$1]} ] ; then rm -rf ${B[$1]} ; fi

    mkdir ${B[$1]}
    cd ${B[$1]}
    export PKG_CONFIG_PATH=/usr/${libdir}/pkgconfig:/usr/share/pkgconfig
    export LDFLAGS='-Wl,--as-needed' 
    ${S[$1]}/autogen.sh --prefix=/usr --libdir=/usr/${libdir} --sysconfdir=/etc \
	    --localstatedir=/var --infodir='${prefix}'/share/info --mandir='${prefix}'/share/man ${OPT_CONFIG[$1]}
    if [ $? != 0 ]; then
	echo "configure error. $0 script stop"
	exit 255
    fi
}

do_build() {
    cd ${B[$1]}
    export LDFLAGS='-Wl,--as-needed'
    make
    if [ $? != 0 ]; then
	echo "make error. $0 script stop"
	exit 255
    fi
}

do_install() {
    cd ${B[$1]}
    export LDFLAGS='-Wl,--as-needed'
    make install DESTDIR=$P HTML_DIR=/usr/share/doc/libxml2/html \
         DOC_MODULE=libxml2
    if [ $? != 0 ]; then
	echo "make install error. $0 script stop"
	exit 255
    fi

    # add extra func
    mkdir -p $docdir/$src
    mv $docdir/libxml2/examples $docdir/$src
    mv $docdir/libxml2/html $docdir/$src/doc
    mv $docdir/libxml2-python-$vers $docdir/$src/python
    mv $docdir/$src/{examples,{Copyright,TODO_SCHEMAS,README.tests}.gz} $C
    mv $docdir/$src/{doc,python,$myname.gz} $C
    mv $C/{examples,{Copyright,TODO_SCHEMAS,README.tests}.gz} $docdir/$src
    mv $C/{doc,python,$myname.gz} $docdir/$src
}

do_package() {
    for i in $pkgbase ; do
        cd $P
        /sbin/makepkg $W/$pkg.$compress <<EOF
y
1
EOF
    done
}

source /usr/src/qbilinux/PackageBuild.func
